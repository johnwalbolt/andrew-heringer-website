# Password that protects the live site (HTTP Basic Auth).
# Set this in Vercel → Project → Settings → Environment Variables.
# If unset, the fallback in middleware.ts is used.
SITE_PASSWORD=your-password-here
